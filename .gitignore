# Environment / secrets
.env
.env.*

# Database credentials – use environment variables instead (see config/database.php)
# config/database.php is intentionally tracked but must NOT contain real credentials

# PHP error logs
error_log
*.log
api/payroll/error_log

# Composer dependencies (install with: composer install)
vendor/

# OS / editor artifacts
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
